name: CD

on:
  pull_request:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Baixar o código
      uses: actions/checkout@v3

    - name: Simular Deploy
      run: echo "Iniciando deploy da API de Gestão de Documentos..."